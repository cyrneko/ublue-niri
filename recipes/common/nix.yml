modules:
  - type: script
    snippets:
      - "curl -fsSL https://install.determinate.systems/nix | sh -s -- install --no-start-daemon --no-confirm"
      # what fucking order do these args have to be in lmfao help
